# D:\电器政策知识图谱构建 — 目录说明（AGENTS）

本文件用于快速理解当前数据目录的结构、内容分布、文件夹关系与潜在数据问题，便于后续做知识图谱抽取、清洗与去重。

## 总览
- 顶层共有 5 个目录，全部为政策文本（.txt）集合。
- 命名习惯：多为“YYYY年MM月DD日 + 发布机构 + 政策标题”。
- 主题大致分两类：
  1) 电价政策（分时电价、阶梯电价）
  2) 电气化/电能替代相关政策（含清洁取暖、岸电、清洁能源、设备更新等）

## 目录结构与内容说明

### 1) `分时电价政策`
- 文件数：43（其中包含 1 个 `.DS_Store` 为 macOS 元数据文件，可忽略）。
- 内容范围：工商业/居民峰谷分时电价机制、优化分时电价政策、EV 充电分时电价等。
- 时间跨度：2019–2024（以文件名为准）。

### 2) `阶梯电价政策`
- 文件数：39。
- 内容范围：居民生活用电阶梯电价、“一户多人口”电价政策，以及钢铁/水泥/电解铝等高耗能行业差别/阶梯电价政策。
- 时间跨度：2011–2024。

### 3) `最全`
- 文件数：81。
- 关系：**等同于 `分时电价政策` + `阶梯电价政策` 的合并集**（不包含 `.DS_Store`）。
- 作用：作为电价政策的“全集”目录，适合直接用于电价政策类的统一处理。

### 4) `电气化、电能替代txt文件`
- 文件数（含子目录）：105；子目录 3 个。
- 目录内部结构：
  - `电气化、电能替代txt文件`（内层目录，63 个文本）：
    - 主要为电能替代、清洁取暖、岸电、清洁能源价格政策等核心政策文件。
  - `txt文件\txt文件`（约 35 个文本）：
    - 与内层目录内容高度重叠，像是从压缩包中解压的子集。
  - `txt文件.zip`：
    - 与 `txt文件\txt文件`内容对应的压缩包。
  - `电气化、电能替代txt文件_uni0.txt` ~ `uni5.txt`：
    - 6 个“拼接/汇总”文本，包含多个文件内容拼接（带 `<h2>file:...` 标记）。
    - 文字存在明显乱码（疑似编码混用，例如 GBK/UTF-8 错配），需后续清洗。

### 5) `电气化、电能替代txt文件最全的`
- 文件数：79。
- 关系：与 `电气化、电能替代txt文件\电气化、电能替代txt文件` 高度重合（交集 59 个），
  但**额外包含更宏观/产业层面的政策**（如智能制造、机器人、汽车与家电更新、国办/中办纲领等）。
- 可视为电能替代相关政策的“扩展版/更大范围合集”。

## 文件夹之间的关系（重点）
- `最全` = `分时电价政策` + `阶梯电价政策`（仅排除了 `.DS_Store`）。
- `电气化、电能替代txt文件最全的` 是 `电气化、电能替代txt文件\电气化、电能替代txt文件` 的扩展集合：
  - 额外增加多份国家级/产业规划类政策。
  - 也存在同一文件的“命名差异版本”（如标题截断、标点差异）。
- `电气化、电能替代txt文件\txt文件\txt文件` 很可能是 `txt文件.zip` 解压后的子集，
  并与内层目录存在重复。
- `电气化、电能替代txt文件_uni*.txt` 为拼接文本，适合做全文检索或粗粒度抽取，但需先修复编码。

## 数据质量与清洗建议
- **命名截断/不一致**：
  - 例如“通知”被截断为“通.txt”，或“2024-2025”与“2024—2025”共存。
  - 建议建立文件名规范化规则（替换标点、补全后缀、去除重复空格）。
- **重复文件**：
  - 多目录间存在同名或近似同名文件，建议通过哈希或内容相似度去重。
- **编码问题**：
  - `电气化、电能替代txt文件_uni*.txt` 存在明显乱码，可尝试 GBK ↔ UTF-8 重新解码。
- **隐藏文件**：
  - `分时电价政策` 中有 `.DS_Store`，可直接忽略或删除以避免干扰统计。

## 快速选用建议
- **只做电价政策知识图谱**：使用 `最全` 作为单一入口。
- **只做电能替代/清洁取暖等专题**：优先使用 `电气化、电能替代txt文件最全的`。
- **需要最原始/未加工数据**：从 `电气化、电能替代txt文件\电气化、电能替代txt文件` 入手，并保留 `uni*.txt` 作为补充参考。
