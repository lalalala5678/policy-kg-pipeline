# Step2：样本文档抽样与标注规范（doccano）

时间：2026-02-09

## 这一步做了什么
- 全量重读语料目录，确认 Step2 的有效语料范围是 147 份独立政策文本（排除 `02_汇总拼接` 与压缩包目录）。
- 按六类主题进行规则化抽样：分时电价、阶梯电价、差别电价、补贴补助、岸电、清洁取暖。
- 生成去重后的标注样本池，并导出 doccano 可直接导入的种子 JSONL。
- 制定了可执行的标注准则（标签定义、边界规则、冲突裁决、质检门禁）。

## 方法简述（可用于论文“数据与标注”章节）
- 抽样方法：关键词命中（标题/正文）+ 路径优先 + 年份均衡；每主题目标 8 份。
- 抽样策略：
  - 对每篇文档计算主题得分（标题权重高于正文）。
  - 先做“每年优先一篇”的均衡抽样，再按得分补齐。
  - 设定最小得分阈值，避免低相关文档混入主题样本。
- 标注组织：
  - 文档级项目：元信息（发文机关/日期/文号/地区/对象/实施日期）。
  - 条款级项目：机制词、参数值与单位、条件、任务型字段（主体/动作/期限/考核）。

## 产出文件
- 抽样结果：
  - `00_整理记录/step2_theme_sampling.json`
  - `00_整理记录/step2_theme_sampling.md`
- doccano 种子数据：
  - `00_整理记录/step2_doccano_seed_doc_level.jsonl`
  - `00_整理记录/step2_doccano_seed_clause_level.jsonl`
  - `00_整理记录/step2_doccano_labeled_examples.jsonl`
- 标注规范：
  - `00_整理记录/step2_doccano_annotation_guideline.md`
- 生成脚本：
  - `00_整理记录/scripts/build_step2_sampling.py`

## 当前结果（本轮执行）
- 每主题抽样：8 份，共 6 主题。
- 去重样本池：38 份文档。
- doccano 种子规模：
  - 文档级：38 条
  - 条款级：42 条
  - 弱标签示例：12 条（仅作示例，训练前需人工复核）

## 注意事项
- `step2_doccano_labeled_examples.jsonl` 为弱标签自动示例，不可直接当金标准训练集。
- 真正训练集应来自人工复核后的 doccano 导出结果，并按 `train/dev/test` 切分。
- 文号与实施日期等稀疏字段建议在 Step3 后处理阶段继续做文件名补偿与冲突仲裁。
